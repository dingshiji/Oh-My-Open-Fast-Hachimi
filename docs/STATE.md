# 当前可复用状态

2026-09-16 从既有制作经验整理为独立工作台。用户明确排除换声工具相关内容；本仓库只保留日语 VCV + 神经声码器路线。

历史验证使用独立的本地声库、角色与权重；公开仓库不分发这些文件。固定正面角色舞台已清除背景对象、背景材质、背景贴图与旧动画曲线，剩余角色贴图已打包。

独立验证：
- doctor 检查外部工具与私有素材存在。
- 自编 5 音双声部 MusicXML -> MSCZ -> 秒级音符，检查连音归并和音符数。
- 两库 PC-NSF-HiFiGAN 合成干声、4 秒混音、口型 JSON、中文字幕。
- Blender 1080p30 逐帧 4 秒平面视频，白底，无旧背景；FFmpeg 全片解码。
- Demucs 对短例分成六轨。
- Git 候选集审计：大文件、私有资产和本机配置排除；公开源码无旧项目依赖路径。

验证不等于：没有重新评估每首歌的转谱准确率、没有验证 Muscriptor 当前远程接口、没有测试所有操作系统。依赖版本升级后先跑 README 的短例。

新增歌曲状态请写到 projects/<歌名>/STATE.md，别把私有歌曲路径或历史大文件加到公开文档。

2026-09-18 发布整理：实际私有资产和权重已移出工作树；软件可使用忽略的 tools/local。中英文 README、双语 TXT 恢复说明和不依赖声库的程序测试音频已补齐。历史完整流程验证不代表新克隆包含素材或可直接渲染。

2026-09-18 场景依赖移除：默认渲染器改为代码生成的通用双人测试舞台，无私有角色、贴图或场景文件依赖。原版角色外观未包含。doctor --scope video 独立检查视频工具；歌声仍需声库和权重。实测结果见 docs/VIDEO_VALIDATION.json。
